[package]
name = "cdp-protocol-node"
version = "0.4.0"
edition = "2021"
license = "MIT"
description = "Node/Deno/Bun bindings for cdp-driver (Chrome DevTools Protocol client)"
# Ships to npm as a prebuilt .node addon. A cdylib is not consumable as a Rust
# dependency, so there is nothing for crates.io to serve.
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
cdp-driver = { path = "../cdp-protocol" }
napi = { version = "3", default-features = false, features = ["napi6", "async", "serde-json"] }
napi-derive = "3"
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "sync"] }

[build-dependencies]
napi-build = "2"
