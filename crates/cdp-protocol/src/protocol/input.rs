// @generated by `just generate-methods` from protocol/*.json.
// Do not edit by hand; edit xtask/ and regenerate.

//! The `Input` domain.
//!
//! Types, command parameters and event payloads for the `Input`
//! domain. The method-name constants for the same domain are in
//! [`crate::methods`].

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct TouchPoint {
    /// X coordinate of the event relative to the main frame's viewport in CSS
    /// pixels.
    pub x: f64,
    /// Y coordinate of the event relative to the main frame's viewport in CSS
    /// pixels. 0 refers to the top of the viewport and Y increases as it proceeds
    /// towards the bottom of the viewport.
    pub y: f64,
    /// X radius of the touch area (default: 1.0).
    #[serde(rename = "radiusX", default, skip_serializing_if = "Option::is_none")]
    pub radius_x: Option<f64>,
    /// Y radius of the touch area (default: 1.0).
    #[serde(rename = "radiusY", default, skip_serializing_if = "Option::is_none")]
    pub radius_y: Option<f64>,
    /// Rotation angle (default: 0.0).
    #[serde(
        rename = "rotationAngle",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub rotation_angle: Option<f64>,
    /// Force (default: 1.0).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub force: Option<f64>,
    /// The normalized tangential pressure, which has a range of \[-1,1\]
    /// (default: 0).
    ///
    /// Experimental: may change without notice.
    #[serde(
        rename = "tangentialPressure",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub tangential_pressure: Option<f64>,
    /// The plane angle between the Y-Z plane and the plane containing both the
    /// stylus axis and the Y axis, in degrees of the range \[-90,90\], a positive
    /// tiltX is to the right (default: 0)
    #[serde(rename = "tiltX", default, skip_serializing_if = "Option::is_none")]
    pub tilt_x: Option<f64>,
    /// The plane angle between the X-Z plane and the plane containing both the
    /// stylus axis and the X axis, in degrees of the range \[-90,90\], a positive
    /// tiltY is towards the user (default: 0).
    #[serde(rename = "tiltY", default, skip_serializing_if = "Option::is_none")]
    pub tilt_y: Option<f64>,
    /// The clockwise rotation of a pen stylus around its own major axis, in
    /// degrees in the range \[0,359\] (default: 0).
    ///
    /// Experimental: may change without notice.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub twist: Option<i64>,
    /// Identifier used to track touch sources between events, must be unique
    /// within an event.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<f64>,
}

/// Experimental: may change without notice.
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum GestureSourceType {
    /// `default`
    #[serde(rename = "default")]
    Default,
    /// `touch`
    #[serde(rename = "touch")]
    Touch,
    /// `mouse`
    #[serde(rename = "mouse")]
    Mouse,
    /// A value this vendored schema does not list. Chrome adds values ahead of
    /// the published protocol, so an unrecognised one is kept as it arrived
    /// rather than failing the payload it came in.
    #[serde(untagged)]
    Unrecognized(String),
}

#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum MouseButton {
    /// `none`
    #[serde(rename = "none")]
    None,
    /// `left`
    #[serde(rename = "left")]
    Left,
    /// `middle`
    #[serde(rename = "middle")]
    Middle,
    /// `right`
    #[serde(rename = "right")]
    Right,
    /// `back`
    #[serde(rename = "back")]
    Back,
    /// `forward`
    #[serde(rename = "forward")]
    Forward,
    /// A value this vendored schema does not list. Chrome adds values ahead of
    /// the published protocol, so an unrecognised one is kept as it arrived
    /// rather than failing the payload it came in.
    #[serde(untagged)]
    Unrecognized(String),
}

/// UTC time in seconds, counted from January 1, 1970.
pub type TimeSinceEpoch = f64;

/// Experimental: may change without notice.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct DragDataItem {
    /// Mime type of the dragged data.
    #[serde(rename = "mimeType")]
    pub mime_type: String,
    /// Depending of the value of `mimeType`, it contains the dragged link, text,
    /// HTML markup or any other data.
    pub data: String,
    /// Title associated with a link. Only valid when `mimeType` ==
    /// "text/uri-list".
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub title: Option<String>,
    /// Stores the base URL for the contained markup. Only valid when `mimeType`
    /// == "text/html".
    #[serde(rename = "baseURL", default, skip_serializing_if = "Option::is_none")]
    pub base_url: Option<String>,
}

/// Experimental: may change without notice.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct DragData {
    pub items: Vec<DragDataItem>,
    /// List of filenames that should be included when dropping
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub files: Option<Vec<String>>,
    /// Bit field representing allowed drag operations. Copy = 1, Link = 2, Move =
    /// 16
    #[serde(rename = "dragOperationsMask")]
    pub drag_operations_mask: i64,
}

/// Dispatches a drag event into the page.
///
/// Experimental: may change without notice.
///
/// Parameters of `Input.dispatchDragEvent`.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct DispatchDragEventParams {
    /// Type of the drag event.
    pub r#type: DispatchDragEventType,
    /// X coordinate of the event relative to the main frame's viewport in CSS
    /// pixels.
    pub x: f64,
    /// Y coordinate of the event relative to the main frame's viewport in CSS
    /// pixels. 0 refers to the top of the viewport and Y increases as it proceeds
    /// towards the bottom of the viewport.
    pub y: f64,
    pub data: DragData,
    /// Bit field representing pressed modifier keys. Alt=1, Ctrl=2,
    /// Meta/Command=4, Shift=8 (default: 0).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub modifiers: Option<i64>,
}

/// The values `type` accepts on `dispatchDragEvent`.
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum DispatchDragEventType {
    /// `dragEnter`
    #[serde(rename = "dragEnter")]
    DragEnter,
    /// `dragOver`
    #[serde(rename = "dragOver")]
    DragOver,
    /// `drop`
    #[serde(rename = "drop")]
    Drop,
    /// `dragCancel`
    #[serde(rename = "dragCancel")]
    DragCancel,
    /// A value this vendored schema does not list. Chrome adds values ahead of
    /// the published protocol, so an unrecognised one is kept as it arrived
    /// rather than failing the payload it came in.
    #[serde(untagged)]
    Unrecognized(String),
}

impl crate::typed::Command for DispatchDragEventParams {
    const METHOD: &'static str = "Input.dispatchDragEvent";
    type Returns = crate::typed::NoReturns;
}

/// Dispatches a key event to the page.
///
/// Parameters of `Input.dispatchKeyEvent`.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct DispatchKeyEventParams {
    /// Type of the key event.
    pub r#type: DispatchKeyEventType,
    /// Bit field representing pressed modifier keys. Alt=1, Ctrl=2,
    /// Meta/Command=4, Shift=8 (default: 0).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub modifiers: Option<i64>,
    /// Time at which the event occurred.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timestamp: Option<TimeSinceEpoch>,
    /// Text as generated by processing a virtual key code with a keyboard layout.
    /// Not needed for for `keyUp` and `rawKeyDown` events (default: "")
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub text: Option<String>,
    /// Text that would have been generated by the keyboard if no modifiers were
    /// pressed (except for shift). Useful for shortcut (accelerator) key handling
    /// (default: "").
    #[serde(
        rename = "unmodifiedText",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub unmodified_text: Option<String>,
    /// Unique key identifier (e.g., 'U+0041') (default: "").
    #[serde(
        rename = "keyIdentifier",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub key_identifier: Option<String>,
    /// Unique DOM defined string value for each physical key (e.g., 'KeyA')
    /// (default: "").
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub code: Option<String>,
    /// Unique DOM defined string value describing the meaning of the key in the
    /// context of active modifiers, keyboard layout, etc (e.g., 'AltGr')
    /// (default: "").
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key: Option<String>,
    /// Windows virtual key code (default: 0).
    #[serde(
        rename = "windowsVirtualKeyCode",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub windows_virtual_key_code: Option<i64>,
    /// Native virtual key code (default: 0).
    #[serde(
        rename = "nativeVirtualKeyCode",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub native_virtual_key_code: Option<i64>,
    /// Whether the event was generated from auto repeat (default: false).
    #[serde(
        rename = "autoRepeat",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub auto_repeat: Option<bool>,
    /// Whether the event was generated from the keypad (default: false).
    #[serde(rename = "isKeypad", default, skip_serializing_if = "Option::is_none")]
    pub is_keypad: Option<bool>,
    /// Whether the event was a system key event (default: false).
    #[serde(
        rename = "isSystemKey",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub is_system_key: Option<bool>,
    /// Whether the event was from the left or right side of the keyboard. 1=Left,
    /// 2=Right (default: 0).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub location: Option<i64>,
    /// Editing commands to send with the key event (e.g., 'selectAll') (default:
    /// \[\]). These are related to but not equal the command names used in
    /// `document.execCommand` and NSStandardKeyBindingResponding. See
    /// <https://source.chromium.org/chromium/chromium/src/+/main:third_party/blink/renderer/core/editing/commands/editor_command_names.h>
    /// for valid command names.
    ///
    /// Experimental: may change without notice.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub commands: Option<Vec<String>>,
}

/// The values `type` accepts on `dispatchKeyEvent`.
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum DispatchKeyEventType {
    /// `keyDown`
    #[serde(rename = "keyDown")]
    KeyDown,
    /// `keyUp`
    #[serde(rename = "keyUp")]
    KeyUp,
    /// `rawKeyDown`
    #[serde(rename = "rawKeyDown")]
    RawKeyDown,
    /// `char`
    #[serde(rename = "char")]
    Char,
    /// A value this vendored schema does not list. Chrome adds values ahead of
    /// the published protocol, so an unrecognised one is kept as it arrived
    /// rather than failing the payload it came in.
    #[serde(untagged)]
    Unrecognized(String),
}

impl crate::typed::Command for DispatchKeyEventParams {
    const METHOD: &'static str = "Input.dispatchKeyEvent";
    type Returns = crate::typed::NoReturns;
}

/// This method emulates inserting text that doesn't come from a key press,
/// for example an emoji keyboard or an IME.
///
/// Experimental: may change without notice.
///
/// Parameters of `Input.insertText`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct InsertTextParams {
    /// The text to insert.
    pub text: String,
}

impl crate::typed::Command for InsertTextParams {
    const METHOD: &'static str = "Input.insertText";
    type Returns = crate::typed::NoReturns;
}

/// This method sets the current candidate text for IME. Use
/// imeCommitComposition to commit the final text. Use imeSetComposition with
/// empty string as text to cancel composition.
///
/// Experimental: may change without notice.
///
/// Parameters of `Input.imeSetComposition`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct ImeSetCompositionParams {
    /// The text to insert
    pub text: String,
    /// selection start
    #[serde(rename = "selectionStart")]
    pub selection_start: i64,
    /// selection end
    #[serde(rename = "selectionEnd")]
    pub selection_end: i64,
    /// replacement start
    #[serde(
        rename = "replacementStart",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub replacement_start: Option<i64>,
    /// replacement end
    #[serde(
        rename = "replacementEnd",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub replacement_end: Option<i64>,
}

impl crate::typed::Command for ImeSetCompositionParams {
    const METHOD: &'static str = "Input.imeSetComposition";
    type Returns = crate::typed::NoReturns;
}

/// Dispatches a mouse event to the page.
///
/// Parameters of `Input.dispatchMouseEvent`.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct DispatchMouseEventParams {
    /// Type of the mouse event.
    pub r#type: DispatchMouseEventType,
    /// X coordinate of the event relative to the main frame's viewport in CSS
    /// pixels.
    pub x: f64,
    /// Y coordinate of the event relative to the main frame's viewport in CSS
    /// pixels. 0 refers to the top of the viewport and Y increases as it proceeds
    /// towards the bottom of the viewport.
    pub y: f64,
    /// Bit field representing pressed modifier keys. Alt=1, Ctrl=2,
    /// Meta/Command=4, Shift=8 (default: 0).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub modifiers: Option<i64>,
    /// Time at which the event occurred.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timestamp: Option<TimeSinceEpoch>,
    /// Mouse button (default: "none").
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub button: Option<MouseButton>,
    /// A number indicating which buttons are pressed on the mouse when a mouse
    /// event is triggered. Left=1, Right=2, Middle=4, Back=8, Forward=16, None=0.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub buttons: Option<i64>,
    /// Number of times the mouse button was clicked (default: 0).
    #[serde(
        rename = "clickCount",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub click_count: Option<i64>,
    /// The normalized pressure, which has a range of \[0,1\] (default: 0).
    ///
    /// Experimental: may change without notice.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub force: Option<f64>,
    /// The normalized tangential pressure, which has a range of \[-1,1\]
    /// (default: 0).
    ///
    /// Experimental: may change without notice.
    #[serde(
        rename = "tangentialPressure",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub tangential_pressure: Option<f64>,
    /// The plane angle between the Y-Z plane and the plane containing both the
    /// stylus axis and the Y axis, in degrees of the range \[-90,90\], a positive
    /// tiltX is to the right (default: 0).
    #[serde(rename = "tiltX", default, skip_serializing_if = "Option::is_none")]
    pub tilt_x: Option<f64>,
    /// The plane angle between the X-Z plane and the plane containing both the
    /// stylus axis and the X axis, in degrees of the range \[-90,90\], a positive
    /// tiltY is towards the user (default: 0).
    #[serde(rename = "tiltY", default, skip_serializing_if = "Option::is_none")]
    pub tilt_y: Option<f64>,
    /// The clockwise rotation of a pen stylus around its own major axis, in
    /// degrees in the range \[0,359\] (default: 0).
    ///
    /// Experimental: may change without notice.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub twist: Option<i64>,
    /// X delta in CSS pixels for mouse wheel event (default: 0).
    #[serde(rename = "deltaX", default, skip_serializing_if = "Option::is_none")]
    pub delta_x: Option<f64>,
    /// Y delta in CSS pixels for mouse wheel event (default: 0).
    #[serde(rename = "deltaY", default, skip_serializing_if = "Option::is_none")]
    pub delta_y: Option<f64>,
    /// Pointer type (default: "mouse").
    #[serde(
        rename = "pointerType",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub pointer_type: Option<DispatchMouseEventPointerType>,
}

/// The values `type` accepts on `dispatchMouseEvent`.
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum DispatchMouseEventType {
    /// `mousePressed`
    #[serde(rename = "mousePressed")]
    MousePressed,
    /// `mouseReleased`
    #[serde(rename = "mouseReleased")]
    MouseReleased,
    /// `mouseMoved`
    #[serde(rename = "mouseMoved")]
    MouseMoved,
    /// `mouseWheel`
    #[serde(rename = "mouseWheel")]
    MouseWheel,
    /// A value this vendored schema does not list. Chrome adds values ahead of
    /// the published protocol, so an unrecognised one is kept as it arrived
    /// rather than failing the payload it came in.
    #[serde(untagged)]
    Unrecognized(String),
}

/// The values `pointerType` accepts on `dispatchMouseEvent`.
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum DispatchMouseEventPointerType {
    /// `mouse`
    #[serde(rename = "mouse")]
    Mouse,
    /// `pen`
    #[serde(rename = "pen")]
    Pen,
    /// A value this vendored schema does not list. Chrome adds values ahead of
    /// the published protocol, so an unrecognised one is kept as it arrived
    /// rather than failing the payload it came in.
    #[serde(untagged)]
    Unrecognized(String),
}

impl crate::typed::Command for DispatchMouseEventParams {
    const METHOD: &'static str = "Input.dispatchMouseEvent";
    type Returns = crate::typed::NoReturns;
}

/// Dispatches a touch event to the page.
///
/// Parameters of `Input.dispatchTouchEvent`.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct DispatchTouchEventParams {
    /// Type of the touch event. TouchEnd and TouchCancel must not contain any
    /// touch points, while TouchStart and TouchMove must contains at least one.
    pub r#type: DispatchTouchEventType,
    /// Active touch points on the touch device. One event per any changed point
    /// (compared to previous touch event in a sequence) is generated, emulating
    /// pressing/moving/releasing points one by one.
    #[serde(rename = "touchPoints")]
    pub touch_points: Vec<TouchPoint>,
    /// Bit field representing pressed modifier keys. Alt=1, Ctrl=2,
    /// Meta/Command=4, Shift=8 (default: 0).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub modifiers: Option<i64>,
    /// Time at which the event occurred.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timestamp: Option<TimeSinceEpoch>,
}

/// The values `type` accepts on `dispatchTouchEvent`.
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum DispatchTouchEventType {
    /// `touchStart`
    #[serde(rename = "touchStart")]
    TouchStart,
    /// `touchEnd`
    #[serde(rename = "touchEnd")]
    TouchEnd,
    /// `touchMove`
    #[serde(rename = "touchMove")]
    TouchMove,
    /// `touchCancel`
    #[serde(rename = "touchCancel")]
    TouchCancel,
    /// A value this vendored schema does not list. Chrome adds values ahead of
    /// the published protocol, so an unrecognised one is kept as it arrived
    /// rather than failing the payload it came in.
    #[serde(untagged)]
    Unrecognized(String),
}

impl crate::typed::Command for DispatchTouchEventParams {
    const METHOD: &'static str = "Input.dispatchTouchEvent";
    type Returns = crate::typed::NoReturns;
}

/// Cancels any active dragging in the page.
///
/// Parameters of `Input.cancelDragging`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct CancelDraggingParams {}

impl crate::typed::Command for CancelDraggingParams {
    const METHOD: &'static str = "Input.cancelDragging";
    type Returns = crate::typed::NoReturns;
}

/// Emulates touch event from the mouse event parameters.
///
/// Experimental: may change without notice.
///
/// Parameters of `Input.emulateTouchFromMouseEvent`.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct EmulateTouchFromMouseEventParams {
    /// Type of the mouse event.
    pub r#type: EmulateTouchFromMouseEventType,
    /// X coordinate of the mouse pointer in DIP.
    pub x: i64,
    /// Y coordinate of the mouse pointer in DIP.
    pub y: i64,
    /// Mouse button. Only "none", "left", "right" are supported.
    pub button: MouseButton,
    /// Time at which the event occurred (default: current time).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timestamp: Option<TimeSinceEpoch>,
    /// X delta in DIP for mouse wheel event (default: 0).
    #[serde(rename = "deltaX", default, skip_serializing_if = "Option::is_none")]
    pub delta_x: Option<f64>,
    /// Y delta in DIP for mouse wheel event (default: 0).
    #[serde(rename = "deltaY", default, skip_serializing_if = "Option::is_none")]
    pub delta_y: Option<f64>,
    /// Bit field representing pressed modifier keys. Alt=1, Ctrl=2,
    /// Meta/Command=4, Shift=8 (default: 0).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub modifiers: Option<i64>,
    /// Number of times the mouse button was clicked (default: 0).
    #[serde(
        rename = "clickCount",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub click_count: Option<i64>,
}

/// The values `type` accepts on `emulateTouchFromMouseEvent`.
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum EmulateTouchFromMouseEventType {
    /// `mousePressed`
    #[serde(rename = "mousePressed")]
    MousePressed,
    /// `mouseReleased`
    #[serde(rename = "mouseReleased")]
    MouseReleased,
    /// `mouseMoved`
    #[serde(rename = "mouseMoved")]
    MouseMoved,
    /// `mouseWheel`
    #[serde(rename = "mouseWheel")]
    MouseWheel,
    /// A value this vendored schema does not list. Chrome adds values ahead of
    /// the published protocol, so an unrecognised one is kept as it arrived
    /// rather than failing the payload it came in.
    #[serde(untagged)]
    Unrecognized(String),
}

impl crate::typed::Command for EmulateTouchFromMouseEventParams {
    const METHOD: &'static str = "Input.emulateTouchFromMouseEvent";
    type Returns = crate::typed::NoReturns;
}

/// Ignores input events (useful while auditing page).
///
/// Parameters of `Input.setIgnoreInputEvents`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct SetIgnoreInputEventsParams {
    /// Ignores input events processing when set to true.
    pub ignore: bool,
}

impl crate::typed::Command for SetIgnoreInputEventsParams {
    const METHOD: &'static str = "Input.setIgnoreInputEvents";
    type Returns = crate::typed::NoReturns;
}

/// Prevents default drag and drop behavior and instead emits
/// `Input.dragIntercepted` events. Drag and drop behavior can be directly
/// controlled via `Input.dispatchDragEvent`.
///
/// Experimental: may change without notice.
///
/// Parameters of `Input.setInterceptDrags`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct SetInterceptDragsParams {
    pub enabled: bool,
}

impl crate::typed::Command for SetInterceptDragsParams {
    const METHOD: &'static str = "Input.setInterceptDrags";
    type Returns = crate::typed::NoReturns;
}

/// Synthesizes a pinch gesture over a time period by issuing appropriate
/// touch events.
///
/// Experimental: may change without notice.
///
/// Parameters of `Input.synthesizePinchGesture`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct SynthesizePinchGestureParams {
    /// X coordinate of the start of the gesture in CSS pixels.
    pub x: f64,
    /// Y coordinate of the start of the gesture in CSS pixels.
    pub y: f64,
    /// Relative scale factor after zooming (\>1.0 zooms in, \<1.0 zooms out).
    #[serde(rename = "scaleFactor")]
    pub scale_factor: f64,
    /// Relative pointer speed in pixels per second (default: 800).
    #[serde(
        rename = "relativeSpeed",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub relative_speed: Option<i64>,
    /// Which type of input events to be generated (default: 'default', which
    /// queries the platform for the preferred input type).
    #[serde(
        rename = "gestureSourceType",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub gesture_source_type: Option<GestureSourceType>,
}

impl crate::typed::Command for SynthesizePinchGestureParams {
    const METHOD: &'static str = "Input.synthesizePinchGesture";
    type Returns = crate::typed::NoReturns;
}

/// Synthesizes a scroll gesture over a time period by issuing appropriate
/// touch events.
///
/// Experimental: may change without notice.
///
/// Parameters of `Input.synthesizeScrollGesture`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct SynthesizeScrollGestureParams {
    /// X coordinate of the start of the gesture in CSS pixels.
    pub x: f64,
    /// Y coordinate of the start of the gesture in CSS pixels.
    pub y: f64,
    /// The distance to scroll along the X axis (positive to scroll left).
    #[serde(rename = "xDistance", default, skip_serializing_if = "Option::is_none")]
    pub x_distance: Option<f64>,
    /// The distance to scroll along the Y axis (positive to scroll up).
    #[serde(rename = "yDistance", default, skip_serializing_if = "Option::is_none")]
    pub y_distance: Option<f64>,
    /// The number of additional pixels to scroll back along the X axis, in
    /// addition to the given distance.
    #[serde(
        rename = "xOverscroll",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub x_overscroll: Option<f64>,
    /// The number of additional pixels to scroll back along the Y axis, in
    /// addition to the given distance.
    #[serde(
        rename = "yOverscroll",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub y_overscroll: Option<f64>,
    /// Prevent fling (default: true).
    #[serde(
        rename = "preventFling",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub prevent_fling: Option<bool>,
    /// Swipe speed in pixels per second (default: 800).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub speed: Option<i64>,
    /// Which type of input events to be generated (default: 'default', which
    /// queries the platform for the preferred input type).
    #[serde(
        rename = "gestureSourceType",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub gesture_source_type: Option<GestureSourceType>,
    /// The number of times to repeat the gesture (default: 0).
    #[serde(
        rename = "repeatCount",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub repeat_count: Option<i64>,
    /// The number of milliseconds delay between each repeat. (default: 250).
    #[serde(
        rename = "repeatDelayMs",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub repeat_delay_ms: Option<i64>,
    /// The name of the interaction markers to generate, if not empty (default:
    /// "").
    #[serde(
        rename = "interactionMarkerName",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub interaction_marker_name: Option<String>,
}

impl crate::typed::Command for SynthesizeScrollGestureParams {
    const METHOD: &'static str = "Input.synthesizeScrollGesture";
    type Returns = crate::typed::NoReturns;
}

/// Synthesizes a tap gesture over a time period by issuing appropriate touch
/// events.
///
/// Experimental: may change without notice.
///
/// Parameters of `Input.synthesizeTapGesture`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct SynthesizeTapGestureParams {
    /// X coordinate of the start of the gesture in CSS pixels.
    pub x: f64,
    /// Y coordinate of the start of the gesture in CSS pixels.
    pub y: f64,
    /// Duration between touchdown and touchup events in ms (default: 50).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub duration: Option<i64>,
    /// Number of times to perform the tap (e.g. 2 for double tap, default: 1).
    #[serde(rename = "tapCount", default, skip_serializing_if = "Option::is_none")]
    pub tap_count: Option<i64>,
    /// Which type of input events to be generated (default: 'default', which
    /// queries the platform for the preferred input type).
    #[serde(
        rename = "gestureSourceType",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub gesture_source_type: Option<GestureSourceType>,
}

impl crate::typed::Command for SynthesizeTapGestureParams {
    const METHOD: &'static str = "Input.synthesizeTapGesture";
    type Returns = crate::typed::NoReturns;
}

/// Emitted only when `Input.setInterceptDrags` is enabled. Use this data with
/// `Input.dispatchDragEvent` to restore normal drag and drop behavior.
///
/// Experimental: may change without notice.
///
/// Payload of the `Input.dragIntercepted` event.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct DragInterceptedEvent {
    pub data: DragData,
}

impl crate::typed::Event for DragInterceptedEvent {
    const METHOD: &'static str = "Input.dragIntercepted";
}
